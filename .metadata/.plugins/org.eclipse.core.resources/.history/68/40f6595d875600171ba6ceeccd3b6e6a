import environment.Graph
import environment.Node
import environment.Edge

/** 
 * 
 */
/** 
 * @author AG
 * 
 */
class MainTest {
	def main(args : String[]) : void {
		val graph = new Graph
		
		for(var i=0; i<16; i++) {
			graph.addNode(new Node("Node" + i))
		}
		
		graph.addEdge(graph.nodes, graph.nodes[1])
	}
}
